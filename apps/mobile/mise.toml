[tasks]
dev = { run = "flutter run", description = "Run mobile app" }
build = { run = "flutter build", description = "Build mobile app" }
install = { run = "flutter pub get", description = "Install dependencies", sources = ["pubspec.yaml", "pubspec.lock"] }
lint = { run = ["dart format --output=none --set-exit-if-changed .", "flutter analyze --fatal-infos"], description = "Analyze mobile app (format + analyze, matches CI)" }
format = { run = "dart format .", description = "Format mobile app" }
test = { run = "flutter test", description = "Test mobile app" }
"gen:l10n" = { run = "flutter gen-l10n", description = "Generate localizations" }
"gen:api" = { run = "dart run swagger_parser", description = "Generate API client from OpenAPI" }
"test:e2e" = { run = "maestro test .maestro/", description = "Run Maestro E2E tests" }
"test:e2e:smoke" = { run = "maestro test .maestro/ --include-tags=smoke", description = "Run Maestro smoke tests" }
